export interface CanvasData {
    reference: HTMLCanvasElement
    context: CanvasRenderingContext2D
    pixelRatio: number
    CSSWidth: number
    CSSHeight: number
    fullWidth: number
    fullHeight: number
    viewHeight: number
    viewWidth: number
}