export interface Theme {
    chartBackground: string
    axesColor: string
    textColor: string
    chartColor: string
    gridColor: string
    axesWidth: number
    drawAxes: boolean
    drawArrows: boolean
    dotsRadius: number
}
